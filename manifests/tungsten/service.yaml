# In-cluster only: http://tungsten.tungsten.svc.cluster.local. No ingress or tunnel.
apiVersion: v1
kind: Service
metadata:
  name: tungsten
  namespace: tungsten
spec:
  selector:
    app: tungsten
    component: api
  ports:
    - port: 80
      targetPort: 3002
